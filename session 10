import java.util.*;

public class Solution {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        int m = sc.nextInt();

        Deque<Integer> deque = new ArrayDeque<>();
        Set<Integer> set = new HashSet<>();

        int maxUnique = 0;

        for (int i = 0; i < n; i++) {
            int num = sc.nextInt();

            deque.addLast(num);

            if (set.contains(num)) {
                // Already exists in current window
            } else {
                set.add(num);
            }

            // Keep window size exactly m
            if (deque.size() > m) {
                int removed = deque.removeFirst();

                // Check if removed value still exists in the window
                if (!deque.contains(removed)) {
                    set.remove(removed);
                }
            }

            if (deque.size() == m) {
                maxUnique = Math.max(maxUnique, set.size());
            }
        }

        System.out.println(maxUnique);
        sc.close();
    }
}
output;
n = 6, m = 3
[5, 3, 5, 2, 3, 2]
